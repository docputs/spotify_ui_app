import 'package:flutter/material.dart';

class PlaylistAlbumCover extends StatelessWidget {
  const PlaylistAlbumCover({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 200,
      width: 200,
      color: Colors.red,
    );
  }
}
